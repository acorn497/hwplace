/**
 * 캔버스 표시 관련 공용 상수.
 * PixelField(그리는 쪽)와 설정 패널(설명하는 쪽)이 같은 값을 봐야 하므로 여기에 둔다.
 */

/**
 * 그리드를 그리기 시작하는 최소 배율.
 * 이보다 작으면 한 칸이 몇 px밖에 안 돼 선끼리 붙어 캔버스가 회색으로 덮인다.
 */
export const GRID_MIN_ZOOM = 4;
